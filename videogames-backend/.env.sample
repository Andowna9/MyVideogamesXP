# Application secret generated with 'openssl rand -hex 32' command.
# Use the same one for both backends
APP_SECRET=secret_here

# Mongodb connection string
MONGODB_URI=mongodb://mongo:27017/videogames_lists

# Twitch client and igdb api token
CLIENT_ID=client_here
API_TOKEN=token_here